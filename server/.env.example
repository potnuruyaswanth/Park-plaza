# Backend Environment Variables

# Server Config
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb+srv://yaswanthvenkata369_db_user:Yaswanth%401@cluster0.yglriby.mongodb.net/?appName=Cluster0

# JWT Secret Keys
JWT_SECRET=your-jwt-secret-key-change-this-in-production
REFRESH_TOKEN_SECRET=your-refresh-token-secret-key-change-this-in-production

# Razorpay Configuration
# Razorpay Configuration (Optional)
# If you have a Razorpay account, put your keys here. If you don't have an
# account or want to skip payments during development, leave these blank and
# set PAYMENT_ENABLED=false. Razorpay test keys can be used for development.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
PAYMENT_ENABLED=false

# Email Configuration (Optional)
# Example for Gmail: use smtp.gmail.com and an App Password (recommended)
# - Create an App Password if your Google account has 2FA enabled.
# - Alternatives: SendGrid, Mailgun, Amazon SES (recommended for production).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# SMTP_USER should be the full email address used to send mails (e.g. your@example.com)
SMTP_USER=youremail@example.com
# SMTP_PASS should be the account password or an app-specific password (for Gmail App Passwords)
SMTP_PASS=your-email-app-password
SMTP_FROM=Park Plaza <no-reply@parkplaza.local>

# If you don't want to configure email now, leave SMTP_* empty or comment them out.

# Multer Upload Path
UPLOAD_PATH=./uploads

# API URLs
FRONTEND_URL=http://localhost:5173
CLIENT_URL=http://localhost:5173
